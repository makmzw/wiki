%h1= @article.title
%p= @article.content

= link_to "戻る", root_path
= link_to "編集", edit_article_path(@article)
= link_to "削除", article_path(@article), method: :delete, data: { confirm: "Are you sure? " }